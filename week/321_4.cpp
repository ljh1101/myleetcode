// 没做出来
#include <vector>
using namespace std;

class Solution {
  public:
    int countSubarrays(vector<int>& nums, int k)
    {
        int pos = 0;
        int count = 1;
        for (int i = 0; i < nums.size(); i++)
            if (nums[i] == k)
                pos = i;
        while (1) {
        if
        }


    }
};